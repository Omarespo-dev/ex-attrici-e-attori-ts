// 📌 Milestone 1
// Crea un type alias Person per rappresentare una persona generica.
type Person = {
  readonly id: number,
  readonly name: string,
  birth_year: number,
  death_year?: number,
  biography: string,
  image: string
}

